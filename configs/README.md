# Configs

Shared configuration fragments for Claude Code and other AI tools.

Includes `settings.json` snippets, keybinding overrides, MCP server configs, and CLAUDE.md templates.

## Conventions

- Use `.example` suffix for configs containing placeholders (e.g., `settings.example.json`)
- Never commit secrets or API keys — use environment variable references instead
