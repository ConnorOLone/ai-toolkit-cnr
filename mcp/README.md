# MCP

MCP (Model Context Protocol) server configurations and custom server implementations.

## Conventions

- Config files go in `configs/` — this directory is for server source code
- Document required environment variables in each server's README
